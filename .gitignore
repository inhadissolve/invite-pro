# ----------------------------------------------------------------
# 1. 의존성 파일 (필수)
# ----------------------------------------------------------------
# 설치된 라이브러리 파일. 용량이 매우 크며, package.json만 있으면
# 'npm install'로 언제든 재생성 가능합니다.
/node_modules


# ----------------------------------------------------------------
# 2. 빌드 결과물 (필수)
# ----------------------------------------------------------------
# 'npm run build'로 생성되는 배포용 폴더.
# 이 역시 원본 코드만 있으면 언제든 재생성 가능합니다.
/dist
/build


# ----------------------------------------------------------------
# 3. 환경 변수 및 민감한 정보 (보안 - 매우 중요!)
# ----------------------------------------------------------------
# 로컬 개발용 환경 변수 파일.
.env.local
.env.*.local

# ⭐️ 우리가 만든 Firebase 설정 파일 (API 키 포함)
# 이 파일이 GitHub에 공개되면, 다른 사람이 내 Firebase 프로젝트를
# 마음대로 사용할 수 있습니다. (요금 폭탄 위험!)
# 
# ‼️‼️ 주의: 만약 팀원과 공유해야 한다면, 
# 이 파일 대신 .env.example 파일을 만들어서 키 이름만 공유하세요.
/src/api/firebase.js 


# ----------------------------------------------------------------
# 4. 운영체제(OS) 및 에디터(IDE) 설정 파일
# ----------------------------------------------------------------
# macOS
.DS_Store

# Windows
Thumbs.db

# VSCode 에디터 개인 설정
.vscode/
!.vscode/extensions.json

# ----------------------------------------------------------------
# 5. 로그 및 캐시 파일
# ----------------------------------------------------------------
# npm, yarn 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase 로컬 로그
firebase-debug.log*

# Vite 캐시
.vite/